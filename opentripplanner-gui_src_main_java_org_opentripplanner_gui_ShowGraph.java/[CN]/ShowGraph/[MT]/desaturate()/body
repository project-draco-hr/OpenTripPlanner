{
  loadPixels();
  for (int i=0; i < width * height; i++) {
    int c=pixels[i];
    float r=red(c);
    float g=green(c);
    float b=blue(c);
    float avg=(r + g + b) / 3;
    r+=(avg - r) / 20;
    g+=(avg - g) / 20;
    b+=(avg - b) / 20;
    pixels[i]=color(r,g,b);
  }
  updatePixels();
}
