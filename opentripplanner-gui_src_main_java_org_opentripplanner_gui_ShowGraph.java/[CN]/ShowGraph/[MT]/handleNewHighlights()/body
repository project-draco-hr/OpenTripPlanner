{
  desaturate();
  noFill();
  stroke(256,8,0,8);
  strokeWeight(8);
  while (!newHighlightedEdges.isEmpty()) {
    DirectEdge de=newHighlightedEdges.poll();
    if (de != null) {
      drawEdge(de);
      highlightedEdges.add(de);
    }
  }
}
