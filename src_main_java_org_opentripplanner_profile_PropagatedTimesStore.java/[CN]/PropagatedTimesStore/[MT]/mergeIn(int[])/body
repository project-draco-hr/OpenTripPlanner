{
  for (int i=0; i < size; i++) {
    int newValue=news[i];
    if (newValue == 0) {
      continue;
    }
    if (mins[i] > newValue) {
      mins[i]=newValue;
    }
    if (maxs[i] < newValue) {
      maxs[i]=newValue;
    }
    sums[i]+=newValue;
    counts[i]+=1;
  }
}
