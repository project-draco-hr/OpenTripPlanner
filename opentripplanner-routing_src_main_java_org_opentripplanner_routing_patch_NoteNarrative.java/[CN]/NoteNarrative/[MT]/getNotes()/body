{
  Set<Alert> baseNotes=base.getNotes();
  HashSet<Alert> notes;
  if (baseNotes != null) {
    notes=new HashSet<Alert>(baseNotes);
  }
 else {
    notes=new HashSet<Alert>(1);
  }
  notes.add(note);
  return notes;
}
