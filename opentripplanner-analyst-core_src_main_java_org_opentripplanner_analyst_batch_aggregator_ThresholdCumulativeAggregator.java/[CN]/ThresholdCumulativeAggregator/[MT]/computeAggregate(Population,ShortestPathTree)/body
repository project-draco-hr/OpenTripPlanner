{
  double result=0;
  for (  Individual destination : destinations) {
    double t=destination.sample.eval(spt);
    if (t < thresholdSeconds)     result+=destination.data * (thresholdSeconds - t);
  }
  return result;
}
