{
  double[] hourBins=new double[7 * 24];
  for (int i=0; i < hourBins.length; i++) {
    if (i == 8)     hourBins[i]=Double.NaN;
 else     if (i == 9)     hourBins[i]=6.1;
 else     hourBins[i]=33.3;
  }
  double avg=1.33;
  return new SegmentSpeedSample(avg,hourBins);
}
