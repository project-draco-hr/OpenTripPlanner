{
  this.options=s.getOptions();
  this.useTransit=options.getModes().isTransit();
  this.maxSpeed=getMaxSpeed(options);
  return distanceLibrary.fastDistance(s.getVertex().getCoordinate(),target.getCoordinate()) / maxSpeed;
}
