{
switch (field) {
case NEAREST_VERTEX:
    if (value == null) {
      unsetNearest_vertex();
    }
 else {
      setNearest_vertex((org.opentripplanner.api.thrift.definition.GraphVertex)value);
    }
  break;
}
}
