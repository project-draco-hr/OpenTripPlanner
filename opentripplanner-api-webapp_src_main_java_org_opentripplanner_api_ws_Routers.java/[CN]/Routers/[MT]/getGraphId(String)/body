{
  Graph graph=graphService.getGraph(routerId);
  if (graph == null)   return Response.status(Status.NOT_FOUND).entity("graph id not registered.").build();
 else   return Response.status(Status.OK).entity(graph.toString()).build();
}
