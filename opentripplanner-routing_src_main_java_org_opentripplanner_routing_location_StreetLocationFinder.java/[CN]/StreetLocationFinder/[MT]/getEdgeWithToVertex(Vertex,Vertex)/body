{
  for (  Edge street : a.getOutgoing()) {
    if (street.getToVertex() == targetToVertex)     return street;
  }
  for (  Edge street : a.getIncoming()) {
    if (street.getToVertex() == targetToVertex)     return street;
  }
  return null;
}
