{
  boolean retVal=false;
  if (TraverseMode.WALK.toString().equals(this.mode) && (this.walkSteps == null || this.walkSteps.size() <= 1) && this.distance == 0) {
    retVal=true;
  }
  return retVal;
}
