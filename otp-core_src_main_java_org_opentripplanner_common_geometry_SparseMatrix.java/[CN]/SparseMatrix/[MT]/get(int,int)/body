{
  x+=INDEX_OFFSET;
  y+=INDEX_OFFSET;
  long x0=x >> shift;
  long y0=y >> shift;
  Long key=x0 + (y0 << 32);
  T[] ts=chunks.get(key);
  if (ts == null) {
    return null;
  }
  int index=((x & mask) << shift) + (y & mask);
  return ts[index];
}
