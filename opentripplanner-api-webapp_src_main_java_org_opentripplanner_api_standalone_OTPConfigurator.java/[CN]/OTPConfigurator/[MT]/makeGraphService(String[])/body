{
  GraphServiceImpl graphService=new GraphServiceImpl();
  if (args.length > 0)   graphService.setPath(args[0]);
 else   graphService.setPath(DEFAULT_GRAPH_LOCATION);
  if (args.length > 1)   graphService.setDefaultRouterId(args[1]);
  return graphService;
}
