{
  Map<TripPattern,TripTimeSubset> ret=Maps.newHashMap();
  for (  TripPattern tp : graph.index.patternForId.values()) {
    TripTimeSubset tts=create(graph,tp,date,startTime,endTime);
    if (tts != null)     ret.put(tp,tts);
  }
  return ret;
}
