{
  if (hasDelay())   return String.format("Update trip %s Stop #%d:%s (%s) delay %s",tripId,stopSeq,stopId,status,delay);
  return String.format("Update trip %s Stop #%d:%s (%s) A%s D%s",tripId,stopSeq,stopId,status,arrive,depart);
}
