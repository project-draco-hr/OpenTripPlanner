{
  if (params.visualize) {
    @SuppressWarnings("unused") OTPComponentProviderFactory cpf=getComponentProviderFactory();
    GraphVisualizer visualizer=new GraphVisualizer(getGraphService());
    return visualizer;
  }
 else   return null;
}
