{
  for (  Individual i : this.individuals) {
    Sample s=i.sample;
    long t=Long.MAX_VALUE;
    if (s == null)     t=-2;
 else     t=s.eval(spt);
    if (t == Long.MAX_VALUE)     t=-1;
    i.output=t;
  }
}
