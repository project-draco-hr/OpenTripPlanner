{
  String query="query Agency{" + "    agency(id: \"agency\"){" + "        name"+ "    }"+ "}";
  Map result=graph.index.graphQL.execute(query).getData();
  assertEquals("Fake Agency",((Map)result.get("agency")).get("name"));
}
