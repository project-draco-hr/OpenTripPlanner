{
  int out=0;
  if (mode.getTram() | mode.getRail() | mode.getSubway()| mode.getFunicular()| mode.getGondola()) {
    out|=StreetEdge.CLASS_TRAIN_PLATFORM;
  }
  if (mode.getBus() | mode.getCableCar()) {
    out|=StreetEdge.CLASS_OTHER_PLATFORM;
  }
  return out;
}
