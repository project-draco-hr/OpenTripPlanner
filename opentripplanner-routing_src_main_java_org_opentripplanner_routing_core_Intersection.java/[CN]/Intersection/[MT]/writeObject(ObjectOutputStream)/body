{
  for (int i=0; i < vertices.size(); i++) {
    if (!(vertices.get(i) instanceof DummyReferenceVertex))     vertices.set(i,new DummyReferenceVertex(vertices.get(i).getLabel()));
  }
  out.defaultWriteObject();
}
