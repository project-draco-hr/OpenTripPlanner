{
  double minDist=Float.MAX_VALUE;
  Vertex ret=null;
  Coordinate c=new Coordinate(lon,lat);
  for (  Vertex vv : this) {
    double dist=vv.distance(c);
    if (dist < minDist) {
      ret=vv;
      minDist=dist;
    }
  }
  return ret;
}
