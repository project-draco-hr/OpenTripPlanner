{
  RoutingRequest options=(new RoutingRequestBuilder(prototypeRoutingRequest)).addTripParameters(trip).setGraph(graphService.getGraph()).setNumItineraries(pathOptions.getNum_paths()).build();
  options.setRouterId("");
  List<GraphPath> paths=pathService.getPaths(options);
  TripPathsExtension tripPaths=new TripPathsExtension(trip,paths);
  options.cleanup();
  return tripPaths;
}
