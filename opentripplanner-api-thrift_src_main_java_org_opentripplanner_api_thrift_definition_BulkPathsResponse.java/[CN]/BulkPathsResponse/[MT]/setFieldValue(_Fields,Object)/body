{
switch (field) {
case PATHS:
    if (value == null) {
      unsetPaths();
    }
 else {
      setPaths((List<org.opentripplanner.api.thrift.definition.TripPaths>)value);
    }
  break;
case COMPUTE_TIME_MILLIS:
if (value == null) {
  unsetCompute_time_millis();
}
 else {
  setCompute_time_millis((Long)value);
}
break;
}
}
