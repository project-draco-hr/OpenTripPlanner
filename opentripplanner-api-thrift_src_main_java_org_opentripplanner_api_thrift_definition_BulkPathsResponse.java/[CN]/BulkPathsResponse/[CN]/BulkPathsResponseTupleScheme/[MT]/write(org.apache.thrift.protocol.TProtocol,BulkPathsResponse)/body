{
  TTupleProtocol oprot=(TTupleProtocol)prot;
{
    oprot.writeI32(struct.paths.size());
    for (    org.opentripplanner.api.thrift.definition.TripPaths _iter12 : struct.paths) {
      _iter12.write(oprot);
    }
  }
}
