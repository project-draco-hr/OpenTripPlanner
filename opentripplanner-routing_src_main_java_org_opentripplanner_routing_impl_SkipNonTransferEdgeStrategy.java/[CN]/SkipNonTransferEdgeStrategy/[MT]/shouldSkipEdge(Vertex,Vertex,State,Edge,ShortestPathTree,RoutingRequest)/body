{
  final Vertex toVertex=edge.getToVertex();
  if (distanceLibrary.fastDistance(toVertex.getCoordinate(),finalTarget) + current.getWalkDistance() > maxWalkDistance)   return !transitLocalStreets.transferrable(toVertex);
 else   return false;
}
