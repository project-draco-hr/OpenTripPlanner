{
  if (--sleepAfter <= 0) {
    sleepAfter=SLEEP_AFTER;
    try {
      Thread.sleep(SLEEP_LEN);
    }
 catch (    InterruptedException e) {
      e.printStackTrace();
    }
  }
  gui.updateTrunkiness(state);
}
