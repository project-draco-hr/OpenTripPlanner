{
  if (request.getWheelchair()) {
    if (!pathservice.isAccessible(request.getFrom(),options) || !pathservice.isAccessible(request.getTo(),options)) {
      throw new LocationNotAccessible();
    }
  }
}
