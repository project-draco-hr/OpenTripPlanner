{
  if (section.getGeometry() != null)   return section.getGeometry();
  Vector<NarrativeItem> items=section.getItems();
  NarrativeItem item=first ? items.get(0) : items.get(items.size() - 1);
  return item.getGeometry();
}
