{
  State s0=spt.getState(v0);
  State s1=spt.getState(v1);
  long m0=Long.MAX_VALUE;
  long m1=Long.MAX_VALUE;
  if (s0 != null)   m0=(s0.getActiveTime() + t0);
  if (s1 != null)   m1=(s1.getActiveTime() + t1);
  return (m0 < m1) ? m0 : m1;
}
