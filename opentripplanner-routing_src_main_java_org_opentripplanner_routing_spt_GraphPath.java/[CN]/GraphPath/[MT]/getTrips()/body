{
  List<AgencyAndId> ret=new LinkedList<AgencyAndId>();
  for (  State s : states) {
    Edge e=s.getBackEdge();
    if (e == null)     continue;
    Trip trip=e.getTrip();
    if (trip != null)     ret.add(trip.getId());
  }
  return ret;
}
