{
  patternsForRoute=graph.getIndex().patternsForRoute;
  patterns.addAll(patternsForRoute.values());
  LOG.info("Number of patterns is {}",patterns.size());
  patternsForStop=graph.getIndex().patternsForStop;
  findTransfers();
}
