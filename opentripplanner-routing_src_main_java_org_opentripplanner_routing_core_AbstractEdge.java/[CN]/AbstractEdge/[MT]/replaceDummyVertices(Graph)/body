{
  if (fromv instanceof DummyReferenceVertex)   fromv=graph.getVertex(((DummyReferenceVertex)fromv).getLabel());
  if (tov instanceof DummyReferenceVertex)   tov=graph.getVertex(((DummyReferenceVertex)tov).getLabel());
}
