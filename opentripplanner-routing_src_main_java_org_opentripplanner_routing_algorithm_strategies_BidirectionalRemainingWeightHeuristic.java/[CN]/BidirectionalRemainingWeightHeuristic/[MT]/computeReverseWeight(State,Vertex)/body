{
  if (s.getVertex() instanceof StreetLocation)   return 0;
  if (s.getWeight() < 10 * 60)   return 0;
  int index=s.getVertex().getIndex();
  if (index < weights.length) {
    double h=weights[index];
    return h;
  }
 else   return 0;
}
