{
  if (graph.getService(StreetVertexIndexService.class) == null) {
  }
  StreetUtils.pruneFloatingIslands(graph,maxIslandSize,islandWithStopMaxSize,LoggerAppenderProvider.createCsvFile4LoggerCat(islandLogFile,"islands"));
  transitToStreetNetwork.buildGraph(graph,extra);
}
