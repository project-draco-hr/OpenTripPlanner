{
  Set<Alert> notes=base.getNotes();
  if (notes == null) {
    return extraNotes;
  }
  if (extraNotes == null) {
    return notes;
  }
  notes=new HashSet<Alert>(notes);
  notes.addAll(extraNotes);
  return notes;
}
