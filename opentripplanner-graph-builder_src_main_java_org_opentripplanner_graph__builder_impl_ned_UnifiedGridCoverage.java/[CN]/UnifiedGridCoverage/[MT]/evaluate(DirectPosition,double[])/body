{
  double[] result=evaluate_orig(point,values);
  if (result == null) {
    result=evaluate_orig(new DirectPosition2D(point.getCoordinate()[0] + OFFSET_HACK,point.getCoordinate()[1] + OFFSET_HACK),values);
  }
  return result;
}
