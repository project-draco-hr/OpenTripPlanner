{
  url=new URL(preferences.get("url",null));
  featureType=preferences.get("featureType",null);
  this.graph=graph;
  LOG.info("Configured WFS polling updater: frequencySec={}, url={} and featureType={}",frequencySec,url.toString(),featureType);
}
