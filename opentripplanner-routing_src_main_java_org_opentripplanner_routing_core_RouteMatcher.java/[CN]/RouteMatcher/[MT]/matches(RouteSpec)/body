{
  if (this == EMPTY_MATCHER)   return false;
  String routeName=routeSpec.routeName.replace("_"," ");
  if (agencyAndRouteIds.contains(new AgencyAndId(routeSpec.agency,routeSpec.routeId)))   return true;
  if (agencyIdAndRouteNames.contains(new T2<String,String>(routeSpec.agency,routeName)))   return true;
  if (routeNames.contains(routeName))   return true;
  return false;
}
