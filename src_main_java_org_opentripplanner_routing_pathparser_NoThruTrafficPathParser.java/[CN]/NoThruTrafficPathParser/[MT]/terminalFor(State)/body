{
  if (state.getBackEdge() instanceof StreetEdge) {
    if (((StreetEdge)state.getBackEdge()).isNoThruTraffic()) {
      return NOTRAFFIC;
    }
 else {
      return REGULAR;
    }
  }
 else {
    return TRANSIT;
  }
}
