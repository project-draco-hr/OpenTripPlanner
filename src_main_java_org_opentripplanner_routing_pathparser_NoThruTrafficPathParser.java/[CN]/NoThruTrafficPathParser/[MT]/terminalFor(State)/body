{
  if (state.getBackEdge() instanceof PlainStreetEdge) {
    if (((PlainStreetEdge)state.getBackEdge()).isNoThruTraffic()) {
      return NOTRAFFIC;
    }
 else {
      return REGULAR;
    }
  }
 else {
    return TRANSIT;
  }
}
