{
  if (timeMode) {
    setTransformation(left,start,right,end);
  }
 else {
    setTransformation(left,bottom,right,top);
  }
  background(255);
  strokeWeight(0.1f);
  drawGeoms();
  strokeWeight(0.1f);
}
