{
  if (time < matrix[current].get(t) && time < bestStops.get(t) && time < maxTime) {
    matrix[current].put(t,time);
    if (!transfer)     bestStops.put(t,time);
    return true;
  }
  return false;
}
