{
  int stop=hop + 1;
  if (stop < offset)   return TripTimes.PASSED;
  if (stop >= offset + departures.length)   return super.getArrivalTime(hop);
  if (arrivals == null)   return departures[stop];
  return arrivals[stop];
}
