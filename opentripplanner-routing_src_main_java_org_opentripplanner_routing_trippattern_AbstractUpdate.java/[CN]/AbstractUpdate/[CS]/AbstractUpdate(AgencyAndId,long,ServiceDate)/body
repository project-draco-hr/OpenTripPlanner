{
  if (tripId == null)   throw new IllegalArgumentException("A tripId must be specified");
  if (serviceDate == null)   throw new IllegalArgumentException("A serviceDate must be specified");
  this.tripId=tripId;
  this.timestamp=timestamp;
  this.serviceDate=serviceDate;
}
