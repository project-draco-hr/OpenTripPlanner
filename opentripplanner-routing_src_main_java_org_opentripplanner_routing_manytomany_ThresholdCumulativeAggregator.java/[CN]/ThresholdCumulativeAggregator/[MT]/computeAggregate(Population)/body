{
  double result=0;
  for (  Individual destination : destinations.elements)   if (destination.result < thresholdSeconds)   result+=destination.data * (thresholdSeconds - destination.result);
  return result;
}
