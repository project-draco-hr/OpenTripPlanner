{
switch (field) {
case LOCATION:
    return getLocation();
case ALLOWED_MODES:
  return getAllowed_modes();
case MAX_EDGES:
return Integer.valueOf(getMax_edges());
}
throw new IllegalStateException();
}
