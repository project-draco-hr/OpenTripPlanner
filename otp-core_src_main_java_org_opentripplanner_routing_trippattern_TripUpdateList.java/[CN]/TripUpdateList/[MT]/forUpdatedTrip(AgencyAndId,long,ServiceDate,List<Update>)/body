{
  if (updates == null || updates.isEmpty())   throw new IllegalArgumentException("At least one update needs to be supplied.");
  return new TripUpdateList(tripId,timestamp,serviceDate,Status.MODIFIED,updates,null);
}
