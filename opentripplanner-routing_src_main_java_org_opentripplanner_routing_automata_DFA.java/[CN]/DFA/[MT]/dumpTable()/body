{
  StringBuilder sb=new StringBuilder();
  for (  int[] row : table) {
    for (    int i : row) {
      if (i == AutomatonState.REJECT)       sb.append("-- ");
 else       sb.append(String.format("%02d ",i));
    }
    sb.append("\n");
  }
  return sb.toString();
}
