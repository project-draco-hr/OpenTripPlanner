{
  List<AutomatonState> newStates=new LinkedList<AutomatonState>();
  for (  AutomatonState as : this)   for (  Transition ae : as)   if (ae.token == Transition.EPSILON)   newStates.add(ae.target);
  this.addAll(newStates);
}
