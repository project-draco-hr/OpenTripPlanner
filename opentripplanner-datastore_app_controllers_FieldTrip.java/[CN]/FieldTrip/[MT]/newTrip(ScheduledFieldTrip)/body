{
  trip.id=null;
  trip.serviceDay=trip.departure;
  User user=getUser();
  if (!user.canScheduleFieldTrips()) {
    trip.groupItineraries.clear();
  }
  trip.save();
  Long id=trip.id;
  renderJSON(id);
}
