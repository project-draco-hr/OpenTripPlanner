{
  int nRemoved=0;
  if (origin != null)   nRemoved+=origin.removeTemporaryEdges(graph);
  if (target != null)   nRemoved+=target.removeTemporaryEdges(graph);
  for (  Vertex v : intermediateVertices)   nRemoved+=v.removeTemporaryEdges(graph);
  return nRemoved;
}
