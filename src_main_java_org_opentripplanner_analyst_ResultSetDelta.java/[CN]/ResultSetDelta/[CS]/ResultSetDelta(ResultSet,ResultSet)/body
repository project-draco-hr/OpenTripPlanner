{
  if (result1.times.length != result2.times.length)   throw new IllegalArgumentException("Result sets do not match when constructing delta!");
  this.times=result1.times;
  this.times2=result2.times;
  buildDelta();
}
