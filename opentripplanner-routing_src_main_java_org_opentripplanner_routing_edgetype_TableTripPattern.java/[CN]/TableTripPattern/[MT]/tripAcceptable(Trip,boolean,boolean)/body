{
  boolean result=true;
  if (wheelchair)   result&=trip.getWheelchairAccessible() == 1;
  if (bicycle)   result&=trip.getTripBikesAllowed() == 2 || (trip.getRoute().getBikesAllowed() == 2 && trip.getTripBikesAllowed() != 1);
  return result;
}
