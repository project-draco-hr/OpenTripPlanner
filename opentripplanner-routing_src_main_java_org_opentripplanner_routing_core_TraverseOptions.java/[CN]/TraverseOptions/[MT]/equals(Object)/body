{
  if (o instanceof TraverseOptions) {
    TraverseOptions to=(TraverseOptions)o;
    return speed == to.speed && modes.equals(to.modes) && back == to.back && wheelchairAccessible == to.wheelchairAccessible && optimizeFor == to.optimizeFor && maxWalkDistance == to.maxWalkDistance && optimizeTransferPenalty == to.optimizeTransferPenalty && maxSlope == to.maxSlope && walkReluctance == to.walkReluctance && boardCost == to.boardCost && bannedRoutes.equals(to.bannedRoutes);
  }
  return false;
}
