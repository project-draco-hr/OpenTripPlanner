{
  if (mode == TraverseMode.WALK)   return walkSpeed;
  if (mode == TraverseMode.BICYCLE)   return bikeSpeed;
  if (mode == TraverseMode.CAR)   return carSpeed;
  throw new IllegalArgumentException("getSpeed(): Invalid mode " + mode);
}
