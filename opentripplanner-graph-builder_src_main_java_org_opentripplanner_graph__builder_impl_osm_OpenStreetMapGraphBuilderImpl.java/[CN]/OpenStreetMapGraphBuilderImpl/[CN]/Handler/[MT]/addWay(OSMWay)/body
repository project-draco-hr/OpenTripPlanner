{
  if (_ways.containsKey(way.getId()))   return;
  if (!way.getTags().containsKey("highway")) {
    return;
  }
  _ways.put(way.getId(),way);
  if (_ways.size() % 1000 == 0)   _log.debug("ways=" + _ways.size());
}
