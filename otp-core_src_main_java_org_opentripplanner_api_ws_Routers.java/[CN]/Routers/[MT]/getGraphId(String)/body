{
  RouterInfo routerInfo=getRouterInfo(routerId);
  if (routerInfo == null)   throw new WebApplicationException(Responses.notFound().entity("Graph id '" + routerId + "' not registered.\n").type("text/plain").build());
  return routerInfo;
}
