{
  return (backEdge == other.getBackEdge() || !(backEdge instanceof PlainStreetEdge) || (((PlainStreetEdge)backEdge).getTurnRestrictions().isEmpty())) && walkDistance <= other.getWalkDistance() * 1.05 && this.getTime() <= other.getTime();
}
