{
  String name=input;
  String place=input;
  if (input.contains("::")) {
    String[] parts=input.split("::");
    name=parts[0];
    place=parts[1];
  }
  return new GenericLocation(name,place);
}
