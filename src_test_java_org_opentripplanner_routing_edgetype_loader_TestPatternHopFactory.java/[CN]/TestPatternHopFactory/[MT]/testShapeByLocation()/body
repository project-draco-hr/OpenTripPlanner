{
  PatternHop hop;
  Geometry geom;
  hop=getHopEdge("G","4");
  geom=hop.getGeometry();
  assertEquals(geom.getLength(),1.16,0.1);
  hop=getHopEdge("A","1");
  geom=hop.getGeometry();
  assertEquals(geom.getLength(),0.01,0.005);
}
