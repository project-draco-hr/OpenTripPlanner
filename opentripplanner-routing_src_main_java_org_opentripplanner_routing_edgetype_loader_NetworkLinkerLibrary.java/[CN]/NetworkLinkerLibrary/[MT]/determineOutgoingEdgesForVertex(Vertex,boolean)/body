{
  Vertex location=getLocation(v);
  if (location == null) {
    getLocation(v);
    return false;
  }
 else {
    graph.addEdge(new StreetTransitLink(location,v,wheelchairAccessible));
    return true;
  }
}
