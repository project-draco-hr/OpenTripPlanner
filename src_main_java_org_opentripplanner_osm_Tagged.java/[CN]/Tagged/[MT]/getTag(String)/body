{
  if (tags == null)   return null;
  for (  String tag : tags.split(";")) {
    String[] kv=tag.split("=",2);
    if (kv[0].equals(key)) {
      if (kv.length == 2) {
        return kv[1];
      }
 else {
        return "";
      }
    }
  }
  return null;
}
