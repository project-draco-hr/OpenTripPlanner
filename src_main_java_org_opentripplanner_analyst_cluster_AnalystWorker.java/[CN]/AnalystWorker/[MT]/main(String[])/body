{
  TaskStatisticsStore tss;
  if (args.length == 2)   tss=new SQSTaskStatisticsStore(args[1]);
 else   tss=new TaskStatisticsStore(){
    @Override public void store(    TaskStatistics ts){
    }
  }
;
  AnalystWorker w=new AnalystWorker(tss);
  if (args.length > 0)   w.BROKER_BASE_URL=args[0];
  w.run();
}
