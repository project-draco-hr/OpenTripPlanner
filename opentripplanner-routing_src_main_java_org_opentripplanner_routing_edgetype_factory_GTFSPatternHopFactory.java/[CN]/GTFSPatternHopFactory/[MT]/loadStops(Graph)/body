{
  for (  Stop stop : _dao.getAllStops()) {
    TransitStop stopVertex=new TransitStop(graph,id(stop.getId()),stop.getLon(),stop.getLat(),stop.getName(),stop.getId(),stop);
    if (stop.getLocationType() != 2) {
      TransitStopArrive arrive=new TransitStopArrive(graph,arrivalVertexId(id(stop.getId())),stop.getLon(),stop.getLat(),stop.getId());
      TransitStopDepart depart=new TransitStopDepart(graph,departureVertexId(id(stop.getId())),stop.getLon(),stop.getLat(),stop.getId());
      new PreAlightEdge(arrive,stopVertex);
      new PreBoardEdge(stopVertex,depart);
    }
  }
}
