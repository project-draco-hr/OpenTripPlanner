{
  int nRemoved=this.destroy();
  if (nRemoved > 0) {
    LOG.warn("Temporary edges were removed during garbage collection. " + "This is probably because a routing context was not properly destroyed.");
  }
}
