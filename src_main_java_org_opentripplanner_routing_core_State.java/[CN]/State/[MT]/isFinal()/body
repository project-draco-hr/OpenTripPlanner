{
  boolean checkPark=stateData.opt.parkAndRide || stateData.opt.kissAndRide;
  if (stateData.opt.arriveBy)   return !isBikeRenting() && !(checkPark && isCarParked());
 else   return !isBikeRenting() && !(checkPark && !isCarParked());
}
