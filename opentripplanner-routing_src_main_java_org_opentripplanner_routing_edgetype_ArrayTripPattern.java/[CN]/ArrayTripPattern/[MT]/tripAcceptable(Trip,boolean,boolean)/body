{
  boolean result=true;
  if (bicycle)   result&=trip.getWheelchairAccessible() == 1;
  if (wheelchair)   result&=trip.getTripBikesAllowed() == 2 || (trip.getRoute().getBikesAllowed() == 2 && trip.getTripBikesAllowed() != 1);
  return result;
}
