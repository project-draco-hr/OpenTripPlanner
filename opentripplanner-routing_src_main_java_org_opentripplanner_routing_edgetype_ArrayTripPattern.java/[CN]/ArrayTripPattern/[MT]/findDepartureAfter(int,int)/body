{
  int low=0;
  int high=trips.length - 1;
  int mid=0;
  while (low <= high) {
    mid=(low + high) >>> 1;
    final int d=tripTimes[mid].departureTimes[stopIndex];
    if (d == key)     return mid;
 else     if (d > key)     high=mid - 1;
 else     low=++mid;
  }
  return mid;
}
