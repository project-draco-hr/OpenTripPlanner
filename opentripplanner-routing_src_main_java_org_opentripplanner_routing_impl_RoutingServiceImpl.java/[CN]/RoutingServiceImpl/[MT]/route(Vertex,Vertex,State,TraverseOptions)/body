{
  HashMap<Vertex,Edge> extraEdges=new HashMap<Vertex,Edge>();
  ShortestPathTree spt=AStar.getShortestPathTree(_graph,fromVertex.getLabel(),toVertex.getLabel(),state,options);
  return spt.getPath(toVertex);
}
