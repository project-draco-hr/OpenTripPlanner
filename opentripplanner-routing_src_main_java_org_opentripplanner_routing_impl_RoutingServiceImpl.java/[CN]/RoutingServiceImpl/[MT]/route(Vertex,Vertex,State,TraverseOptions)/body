{
  HashMap<Vertex,Edge> extraEdges=new HashMap<Vertex,Edge>();
  ShortestPathTree spt=AStar.getShortestPathTree(_graph,fromVertex,toVertex,state,options,extraEdges);
  return spt.getPath(toVertex);
}
