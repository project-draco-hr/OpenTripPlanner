{
  if (X == null)   X=-0.0;
  if (Y == null)   Y=-0.0;
  if (X <= 90.0 && Y <= 180.0) {
    setLon(X);
    setLat(Y);
    geometry=llGeoJSON();
  }
 else {
    setX(X);
    setY(Y);
    geometry=xyGeoJSON();
  }
}
