{
  int ret=0;
  Ride ride=this;
  if (ride.to == null) {
    ride=ride.previous;
  }
  while (ride != null) {
    ret+=ride.rideStats.max;
    ret+=ride.waitStats.max;
    ret+=ride.accessStats.max;
    ride=ride.previous;
  }
  return ret;
}
