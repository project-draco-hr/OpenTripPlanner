{
  int ret=0;
  Ride ride=this;
  if (ride.to == null) {
    ride=ride.previous;
  }
  while (ride != null) {
    ret+=ride.rideStats.min;
    ret+=ride.waitStats.min;
    ret+=ride.accessStats.min;
    ride=ride.previous;
  }
  return ret;
}
