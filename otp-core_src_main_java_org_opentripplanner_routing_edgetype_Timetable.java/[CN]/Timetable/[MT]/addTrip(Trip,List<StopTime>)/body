{
  TripTimes newTripTimes=new TripTimes(trip,stopTimes);
  if (!tripTimes.isEmpty()) {
    TripTimes firstTripTimes=tripTimes.get(0);
    newTripTimes.compactStopSequence(firstTripTimes);
  }
  tripTimes.add(newTripTimes);
}
