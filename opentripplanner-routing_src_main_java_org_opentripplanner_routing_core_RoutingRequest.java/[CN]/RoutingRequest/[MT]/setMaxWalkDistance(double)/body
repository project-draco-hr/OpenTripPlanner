{
  if (maxWalkDistance == 0)   return;
  this.maxWalkDistance=maxWalkDistance;
  if (bikeWalkingOptions != null && bikeWalkingOptions != this) {
    this.bikeWalkingOptions.setMaxWalkDistance(maxWalkDistance);
  }
}
