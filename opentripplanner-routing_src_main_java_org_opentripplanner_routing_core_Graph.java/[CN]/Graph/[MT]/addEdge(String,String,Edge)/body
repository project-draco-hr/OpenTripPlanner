{
  Vertex a=this.getVertex(from_label);
  Vertex b=this.getVertex(to_label);
  if (ee.getFromVertex() != a)   throw new IllegalStateException("Saving an edge with the wrong vertex.");
  addEdge(a,b,ee);
}
