{
  GraphVertex gv=vertices.get(e.getFromVertex().getLabel());
  if (gv != null) {
    gv.removeOutgoing(e);
  }
  gv=vertices.get(e.getToVertex().getLabel());
  if (gv != null) {
    gv.removeIncoming(e);
  }
}
