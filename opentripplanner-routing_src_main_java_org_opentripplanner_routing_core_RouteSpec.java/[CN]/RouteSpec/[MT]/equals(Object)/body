{
  if (other instanceof RouteSpec) {
    RouteSpec otherRs=(RouteSpec)other;
    return ObjectUtils.nullSafeEquals(agency,otherRs.agency) && ObjectUtils.nullSafeEquals(routeName,otherRs.routeName) && ObjectUtils.nullSafeEquals(routeId,otherRs.routeId);
  }
  return false;
}
