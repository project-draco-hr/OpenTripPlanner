{
  ProfileRequest req=profileRouter.request;
  lon=req.from.lon;
  lat=req.from.lat;
  id=makeUniqueId();
  dateTime=req.fromTime;
  routerId=profileRouter.graph.routerId;
  cutoffMinutes=profileRouter.MAX_DURATION / 60;
}
