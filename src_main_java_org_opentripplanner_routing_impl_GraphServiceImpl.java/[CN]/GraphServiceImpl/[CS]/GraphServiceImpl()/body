{
  if (AUTOSCAN_PERIOD_SEC > 0)   scanExecutor.scheduleWithFixedDelay(new Runnable(){
    @Override public void run(){
      autoReloadScan();
    }
  }
,AUTOSCAN_PERIOD_SEC,AUTOSCAN_PERIOD_SEC,TimeUnit.SECONDS);
}
