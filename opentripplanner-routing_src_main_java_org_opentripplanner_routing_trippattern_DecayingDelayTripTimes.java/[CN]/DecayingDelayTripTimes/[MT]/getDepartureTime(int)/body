{
  int stop=hop;
  if (stop < currentStop) {
    if (readThrough)     return super.getDepartureTime(hop);
    return TripTimes.PASSED;
  }
  int t=super.getDepartureTime(hop);
  int elapsed=t - t0;
  return t + decayedDelay(elapsed);
}
